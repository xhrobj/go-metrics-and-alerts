// Package pool предоставляет generic-обёртку над sync.Pool для объектов,
// которые умеют сбрасывать своё состояние.
//
// Pool хранит объекты одного конкретного типа и вызывает Reset перед тем,
// как вернуть объект в базовый sync.Pool.
package pool
